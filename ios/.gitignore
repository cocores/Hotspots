# Generated by `xcodegen generate` — regenerate from project.yml, don't commit.
*.xcodeproj/
.build/
DerivedData/

# Firebase config is per-developer/per-Firebase-project — get your own from
# the Firebase console (see ios/README.md) rather than committing one.
Hotspots/GoogleService-Info.plist

.DS_Store
